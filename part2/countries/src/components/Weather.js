// this won't work without refactoring Country from Countries
// can't call <Weather> other than inside <App>

const Weather = ({}) => {
  //   const submitHandler = (event) => {
  //     event.preventDefault();
  //   };
};

export default Weather;
